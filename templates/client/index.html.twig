{% extends 'base.html.twig' %}

{% block title %}Gestion des Clients - Karhabti{% endblock %}

{% block body %}
<div class="container mt-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1>👥 Gestion des Clients</h1>
        <a href="{{ path('app_client_new') }}" class="btn btn-primary">➕ Ajouter un client</a>
    </div>

    <div class="card shadow-sm">
        <div class="card-body">
            <div class="table-responsive">
                <table class="table table-hover align-middle">
                    <thead class="table-light">
                        <tr>
                            <th>ID</th>
                            <th>Nom</th>
                            <th>Email</th>
                            <th>Téléphone</th>
                            <th>Adresse</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                    {% for client in clients %}
                        <tr>
                            <td>{{ client.id }}</td>
                            <td>{{ client.nom }}</td>
                            <td>{{ client.email }}</td>
                            <td>{{ client.numTeleph }}</td>
                            <td>{{ client.adresse }}</td>
                            <td>
                                <div class="btn-group btn-group-sm">
                                    <a href="{{ path('app_client_show', {'id': client.id}) }}" class="btn btn-outline-info">Voir</a>
                                    <a href="{{ path('app_client_edit', {'id': client.id}) }}" class="btn btn-outline-warning">Éditer</a>
                                </div>
                            </td>
                        </tr>
                    {% else %}
                        <tr>
                            <td colspan="6" class="text-center">Aucun client trouvé.</td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
{% endblock %}
